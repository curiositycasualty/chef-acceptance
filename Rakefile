require "rspec/core/rake_task"

#config = ENV['TEST_CONFIG'] | File.join(__dir__, 'test-config.yml')


RSpec::Core::RakeTask.new do |task|
  task.pattern = 'spec/**/*_spec.rb'
  task.verbose = false
end